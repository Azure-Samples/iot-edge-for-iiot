{
    "$schema-version": "0.0.1",
    "description": "grafana",
    "image": {
        "repository": "${ACR_ADDRESS}/${ACR_REPOSITORY}/grafana",
        "tag": {
            "version": "${CONTAINER_VERSION_TAG}",
            "platforms": {
                "amd64": "./Dockerfile"
            }
        },
        "buildOptions": [],
        "contextPath": "./"
    }
}
