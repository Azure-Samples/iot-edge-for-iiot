{
    "$schema-version": "0.0.1",
    "description": "opcsimulator",
    "image": {
        "repository": "${ACR_ADDRESS}/${ACR_REPOSITORY}/opcsimulator",
        "tag": {
            "version": "${CONTAINER_VERSION_TAG}",
            "platforms": {
                "amd64": "./Dockerfile"
            }
        },
        "buildOptions": [],
        "contextPath": "./"
    }
}
